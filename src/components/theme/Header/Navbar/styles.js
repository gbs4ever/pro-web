import styled from 'styled-components'

export const Wrapper = styled.div`
	padding: 1.5rem 0;
	display: flex;
	align-items: center;
	justify-content: space-between;

	a {
		color: #4ab6d4;
		padding: 7px 7px;
		top: 0;
		flex-direction: row;
		margin: 2px;
		overflow: hidden;
	}
	a:hover {
		color: #fff !important;
		background: #61dafb;
		border-radius: 5px;
	}
`
